# nextcloud-backup-bash
Automated backup script for Nextcloud user data with compression, notification, and cleanup.

# Requirements
- rsync
- tar
- curl
- Pushover account for notifications
- SSH access to the Nextcloud server

# Configuration
Edit the script to set your environment variables
